<!DOCTYPE html>
<html>
<head>
  <title>OP History & Revisit</title>

  <style>
    * {
      box-sizing: border-box;
      font-family: "Segoe UI", Arial, sans-serif;
    }

    body {
      margin: 0;
      min-height: 100vh;
      background: linear-gradient(135deg, #0a6ebd, #5fa8ff);
      display: flex;
      justify-content: center;
      align-items: center;
    }

    /* Top Bar */
    .topbar {
      position: fixed;
      top: 0;
      left: 0;
      width: 100%;
      background: linear-gradient(135deg, #084f8c, #0a6ebd);
      color: white;
      padding: 16px 30px;
      display: flex;
      justify-content: space-between;
      align-items: center;
      box-shadow: 0 4px 15px rgba(0,0,0,0.25);
      z-index: 10;
    }

    .topbar h2 {
      margin: 0;
      font-size: 20px;
      letter-spacing: 0.5px;
    }

    .topbar a {
      color: white;
      text-decoration: none;
      font-weight: bold;
    }

    /* Main Container */
    .history-container {
      background: white;
      width: 1000px;
      max-width: 95%;
      margin-top: 110px;
      padding: 35px 40px 40px;
      border-radius: 18px;
      box-shadow: 0 25px 55px rgba(0,0,0,0.3);
    }

    .section-title {
      font-size: 18px;
      color: #0a6ebd;
      margin-bottom: 18px;
      border-left: 5px solid #0a6ebd;
      padding-left: 10px;
    }

    /* Search Box */
    .search-box {
      display: flex;
      gap: 15px;
      align-items: center;
      margin-bottom: 30px;
    }

    .search-box input {
      flex: 1;
      padding: 12px;
      border-radius: 8px;
      border: 1px solid #ccc;
      font-size: 14px;
    }

    .search-box input:focus {
      outline: none;
      border-color: #0a6ebd;
      box-shadow: 0 0 0 2px rgba(10,110,189,0.15);
    }

    .search-box button {
      padding: 12px 30px;
      border-radius: 25px;
      border: none;
      background: linear-gradient(135deg, #0a6ebd, #084f8c);
      color: white;
      font-size: 14px;
      cursor: pointer;
      font-weight: bold;
      box-shadow: 0 8px 20px rgba(0,0,0,0.25);
    }

    .search-box button:hover {
      transform: translateY(-2px);
    }

    /* Cards */
    .history-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px,1fr));
      gap: 25px;
    }

    .history-card {
      background: #f4f9ff;
      padding: 20px;
      border-radius: 14px;
      box-shadow: 0 10px 25px rgba(0,0,0,0.15);
      position: relative;
    }

    .history-card::before {
      content: "";
      position: absolute;
      left: 0;
      top: 0;
      width: 6px;
      height: 100%;
      background: #0a6ebd;
      border-radius: 14px 0 0 14px;
    }

    .history-card h3 {
      margin: 0;
      color: #0a6ebd;
      font-size: 18px;
    }

    .history-card p {
      margin: 6px 0;
      font-size: 14px;
      color: #444;
    }

    .revisit-btn {
      margin-top: 15px;
      display: inline-block;
      padding: 8px 18px;
      background: #0a6ebd;
      color: white;
      text-decoration: none;
      border-radius: 20px;
      font-size: 14px;
      font-weight: bold;
    }

    .revisit-btn:hover {
      background: #084f8c;
    }

    .no-data {
      text-align: center;
      font-size: 15px;
      color: #666;
      margin-top: 30px;
    }

    @media (max-width: 768px) {
      .search-box {
        flex-direction: column;
      }
    }
  </style>
</head>

<body>

  <!-- Top Bar -->
  <div class="topbar">
    <h2>📂 OP History & Revisit</h2>
    <a href="/dashboard">⬅ Back to Dashboard</a>
  </div>

  <!-- Main Container -->
  <div class="history-container">

    <div class="section-title">Search Patient History</div>

    <form class="search-box" method="POST">
      <input 
        name="mobile" 
        placeholder="Enter patient mobile number (e.g. 9876543210)" 
        required
      >
      <button>Search</button>
    </form>

    <% if (patients && patients.length > 0) { %>

      <div class="section-title">Previous OP Records</div>

      <div class="history-grid">
        <% patients.forEach(p => { %>
          <div class="history-card">
            <h3><%= p.name %></h3>
            <p><b>Mobile:</b> <%= p.mobile %></p>
            <p><b>Complaint:</b> <%= p.complaint || '—' %></p>
            <p><b>Last Visit:</b> <%= new Date(p.created_at).toLocaleDateString() %></p>

            <a href="/op/revisit/<%= p.id %>" class="revisit-btn">
              🔁 Re-Visit
            </a>
          </div>
        <% }) %>
      </div>

    <% } else if (patients) { %>

      <div class="no-data">
        ❌ No OP history found for this mobile number
      </div>

    <% } %>

  </div>

</body>
</html>
